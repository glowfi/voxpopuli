import Voxsphere from '@/components/voxsphere/voxsphere';
import React from 'react';

const page = () => {
    return (
        <div>
            <Voxsphere />
        </div>
    );
};

export default page;
